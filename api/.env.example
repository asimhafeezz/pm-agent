PORT=6000
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5433/pm_agent
JWT_SECRET=dev-secret

# Agent callback base
AGENT_BASE_URL=http://localhost:8000

# Internal service-to-service auth
INTERNAL_API_KEY=dev-internal-key

# Internal URL to integration-service (no /integration suffix)
INTEGRATION_SERVICE_URL=http://localhost:6001

# Secret used to encrypt user integration tokens at rest
INTEGRATION_TOKEN_SECRET=dev-token-secret
INTEGRATION_OAUTH_STATE_SECRET=dev-oauth-state-secret
INTEGRATION_OAUTH_REDIRECT_URI=http://localhost:7001/integrations/callback
# Optional provider-specific override (used for Slack OAuth when set)
SLACK_OAUTH_REDIRECT_URI=

# Auth0 (current login flow)
AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=

# MinIO (file uploads)
MINIO_ENDPOINT=localhost
MINIO_PORT=9001
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=agentpm
